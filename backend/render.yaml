services:
  - type: web
    name: campus-care-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        fromDatabase:
          name: campuscare-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
